---
- name: Deploy PostgreSQL host-based authentication configuration
  become: true
  ansible.builtin.template:
    src: pg_hba.conf.j2
    dest: /pgdata/pg_hba.conf
    owner: postgres
    group: postgres
    mode: "0600"
    backup: true
  notify: Reload PostgreSQL
